<table class="table settings table-bordered table-striped">
  <tbody>
  <% settings.each do |k, v| %>
  <tr>
    <td><%= add_unit(k.to_s.gsub('-', ' ').capitalize) %></td>
    <td><%= v %></td>
  </tr>
  <% end %>
  </tbody>
</table>